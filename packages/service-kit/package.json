{
  "name": "@platform/service-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Shared NestJS scaffolding for Köydaş services — verify-only JWT auth, Prisma + Redis modules, common guards/filters/interceptors/decorators",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@platform/prisma-client": "0.1.0",
    "ioredis": "^5.3.2",
    "passport-jwt": "^4.0.1"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/config": "^3.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/passport": "^10.0.0",
    "passport": "^0.7.0",
    "reflect-metadata": "^0.2.1",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/common": "^10.3.0",
    "@nestjs/config": "^3.2.0",
    "@nestjs/passport": "^10.0.3",
    "@types/node": "^20.11.0",
    "@types/passport-jwt": "^4.0.1",
    "typescript": "^5.4.0"
  }
}
